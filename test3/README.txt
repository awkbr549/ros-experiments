This program compares the velocity model vs the odometry model.

A custom navigation stack was used to test the different implementations.

removed.info contains a list of what was removed to use the navigation directory here as the primary navigation stack

Run with `run.sh'
